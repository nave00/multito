[project]
name = "multito"
version = "0.1.0"
description = "A modular Python library for multi-objective topology optimization."
readme = "README.md"
requires-python = ">=3.8"

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"
